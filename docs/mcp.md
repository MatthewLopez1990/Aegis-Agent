# MCP

The MCP registry stores server definitions:

- Name.
- Command.
- Allowed tools.
- Enabled flag.
- Approval-required flag.
- Metadata.

Servers are disabled by default. Live MCP execution should only be enabled after manifest review, allowed-tool filtering, secret brokering, and policy checks.
